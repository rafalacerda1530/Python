"""
POO - classes

Orientação a objetos, utilizada para fazer modelos do mundo real em forma computacional

Classes podem conter =
    atributos -> representam as caracteristicas do objeto, pelos atributos representamos computacionalmente
    o estado de um objeto

    Metodos(Funções) - > são os comportamentos do objeto, ou seja as ações que ele vai realizar no sistema

Obs: quando definimos as classes do sistemas, chamamos os objetos mapeados para as classes de entidade
    obs: no caso abaixo a lampada é a entidade

pass = quando temos um bloco de código ainda não implementado

OBS: o a letra inicial das classes devem ser maiusculo
"""


class Lampada:
    pass


lamp = Lampada()

print(type(lamp))
